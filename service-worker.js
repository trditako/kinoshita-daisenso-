const CACHE_NAME="kinoshita-daisen-v20";
const APP_SHELL=["./","./index.html","./manifest.json","./icon-192.png","./icon-512.png"];
self.addEventListener('install',e=>{self.skipWaiting();e.waitUntil(caches.open(CACHE_NAME).then(c=>c.addAll(APP_SHELL)))});
self.addEventListener('activate',e=>{e.waitUntil(caches.keys().then(k=>Promise.all(k.filter(x=>x!==CACHE_NAME).map(x=>caches.delete(x)))).then(()=>self.clients.claim()))});
self.addEventListener('fetch',e=>{if(e.request.method!=="GET")return;const u=e.request.url;if(u.includes('/rest/v1/ai_accounts')||u.includes('/rest/v1/player_rankings')){e.respondWith(fetch(e.request));return}e.respondWith(fetch(e.request).then(async r=>{const type=r.headers.get('content-type')||'';if(type.includes('text/html')){const text=await r.clone().text();const p=text.lastIndexOf('</body>');const out=new Response(p<0?text:text.slice(0,p)+'\n'+text.slice(p),{status:r.status,statusText:r.statusText,headers:r.headers});caches.open(CACHE_NAME).then(c=>c.put(e.request,out.clone()));return out}caches.open(CACHE_NAME).then(c=>c.put(e.request,r.clone()));return r}).catch(()=>caches.match(e.request)))});
